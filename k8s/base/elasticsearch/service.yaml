apiVersion: v1 
kind: Service
metadata:
  name: elasticsearch-service
  namespace: ecom-shop 
spec:
  selector:
    app: elasticsearch
  ports:
  - protocol: TCP
    port: 9200
    targetPort: 9200
  type: ClusterIP
  sessionAffinity: None
